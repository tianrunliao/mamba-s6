# Factor Configuration Template for Mamba Crypto Strategy
# This is an empty template. Add your custom factors below.
# Note: The actual factors used in backtesting are not included in this repository.

factors: []
  # Example factor structure (uncomment to use):
  # - name: "example_factor"
  #   calculation: "close.corr(volume)"
  #   direction: 1
  #   description: "Example correlation factor"

processing:
  min_data_points: 30
  winsorization_stds: 3
  entropy_calculation:
    bins: 10
    min_symbols: 3